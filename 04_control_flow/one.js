// let userJustLoggedIn = true
// if (userJustLoggedIn == true) {
//     console.log("hello user");
// }
//  the above code is an "if" statement in js, its only entered the block scope of if ehen the if statement is true
// these below all are the types to comparison we can apply in an if statements
// <, >, ==, <=, >=, !=






// let temperature = 45
// if (temperature >= 46) {
//     console.log("temperature is above 45 degree celcius");
// } else {
//     console.log("temperature is below 45 degree celcius");
// }
//the above code is ifelse statement in js, in this we are having one if condition and the contion is true then it enters the if scope and execute the set of statements. and the condition the is false then it'll automatically executes the sets of statements presents in else scope






// let score = 200
// if (score > 100) {
//     const name = "deepanshu"
//     console.log(`${name} score is ${score}`);
    
// }
// console.log(`${name} score is ${score}`);
// when you run the above code it will gives an error (name is not defined), the reason is beacause of scope level, we defined the name inside of if's block scope and it gives only output for console.log inside of scope and for outside one the name is never defined but if we use var instead of const and let then it'll gives output for outside one too. thats why var is not used nowadays.





// let koeinseggTopSpeed = 450
// if (koeinseggTopSpeed < 100) {
//     console.log(`koeinsegg top speed is ${koeinseggTopSpeed}`)
    
// } else if (koeinseggTopSpeed < 200) {
//     console.log(`koeinsegg top speed is ${koeinseggTopSpeed}`)
    
// }else if (koeinseggTopSpeed < 300) {
//     console.log(`koeinsegg top speed is ${koeinseggTopSpeed}`)
    
// }else if (koeinseggTopSpeed < 400) {
//     console.log(`koeinsegg top speed is ${koeinseggTopSpeed}`)
    
// }
// else {
//      console.log(`koeinsegg top speed is ${koeinseggTopSpeed}`)
    
// }
// the above code is a representation of else if statemets. in this we can apply multiple conditions.






